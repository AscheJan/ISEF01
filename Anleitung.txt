Vorgehen - Wie wurde das Projekt umgesetzt by Jan Asche

    1. Setze das Grundgerüst in Node.js mit Express auf (Backend starten)
    2. Baue die REST-API für Fragenverwaltung (GET, POST, DELETE)
    3. Erstelle eine einfache Quiz-Seite in HTML/CSS/JS
    3. Verbinde das Frontend mit dem Backend (AJAX/Fetch API)
    4. Teste und verbessere das System
    5. Erstelle Dokumentation & Screencast für die Abgabe



    1. Grundgerüst
    #Projektstruktur erstellen
     mkdir quiz-app
     cd quiz-app
     npm init -y

    #Express und weitere Abhängigkeiten installieren
     npm install express mongoose cors dotenv
     npm install nodemon --save-dev
     npm install socket.io
     npm install chalk


    #.env ->
    MONGO_URI=
